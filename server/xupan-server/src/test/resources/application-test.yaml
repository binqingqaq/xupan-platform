spring:
  datasource:
    url: jdbc:h2:mem:xupan;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
xupan:
  automation:
    enabled: false
  auth:
    bootstrap-admin:
      enabled: false

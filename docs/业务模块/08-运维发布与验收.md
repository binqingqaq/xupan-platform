# 业务模块 08：运维、发布与验收

> 当前状态：单服务器交付基础已建立；服务器正式发布和生产验收未完成。

## 1. 当前部署基线

- Java 21。
- Spring Boot 单体服务。
- MySQL 8.4 LTS。
- 单服务器、单实例。
- 配置和密钥分离，生产凭据不入库。
- Flyway 负责数据库迁移。
- Redis、MQ、多实例协调和跨节点广播不属于当前前置条件。

## 2. 发布必须覆盖

```text
构建
  -> 数据库备份
  -> Flyway 前向迁移
  -> 启动/健康检查
  -> 接口和页面冒烟
  -> 日志和指标检查
  -> 失败时回滚或前向修复
```

正式业务发布还需要记录：版本、迁移版本、备份位置和哈希、环境变量、启动停止方式、健康结果、错误日志、回滚/修复结果。

## 3. 验收等级

| 等级 | 能证明什么 | 不能证明什么 |
| --- | --- | --- |
| 静态检查 | 文件、配置和差异基本正确 | 运行时可用 |
| H2 测试 | 领域逻辑、迁移和集成测试通过 | MySQL、网络、生产配置 |
| 本机 MySQL | MySQL 8.4 迁移和本机接口链路 | 服务器和公网 |
| 服务器现场 | 目标服务器部署和运行态 | 正式生产业务稳定性 |
| 生产验证 | 正式环境可用和运维证据 | 未覆盖的长期容量和灾备能力 |

## 4. 当前未完成

- 服务器/生产完整迁移和备份恢复演练。
- HTTPS、Secure Cookie、正式域名和 WebSocket 代理复验。
- systemd、日志滚动、监控告警和故障演练现场验证。
- 长时间期号、结算、BOT 调度和消息失败重试观察。
- 多实例部署下玩家限额和托全局每期上限的强一致控制（当前按执行时统计，极端并发可能少量超出）。
- 玩家工作台新托行为面板的 375px 与桌面截图复验。

## 5. 当前证据快照（2026-09-24）

| 层级 | 当前结果 |
| --- | --- |
| 后端 H2/Flyway | 全量 `285/285` 通过（迁移版本 V1 至 `V32`，共 32 个） |
| 前端 | Vitest `104/104`、`typecheck`、生产构建通过 |
| 本机 MySQL | `8.4.11` 从 V1 迁移到 `V32`，Spring Boot 启动成功，`/actuator/health` 返回 200 |
| 浏览器 | 下注榜、快捷下注、聊天室 4 小时历史、上下分审批、后台赔率与限额配置已完成桌面/375px 验收；托面板以构建产物与单元测试为准 |
| 未执行 | 服务器现场、生产数据库、HTTPS、监控告警联动、故障演练和多实例验证 |

完整的待补事项清单见 [文档索引](../文档索引.md) 的「当前仍在执行或待补的专项计划」。

## 6. 新需求入口

每个功能实施计划必须写明测试等级、现场命令、验收数据、失败恢复和未执行项；没有相应证据时只能标记为代码完成或本机完成。

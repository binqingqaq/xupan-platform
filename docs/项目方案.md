# 首期玩法演示项目方案

## 1. 目标

首期交付一个可在浏览器直接演示的“8 球番摊玩法”页面，复现参考页的核心信息结构和玩法交互：期号、开奖倒计时、8 个位置的 01～20 号码、玩法赔率、按球下注、开奖结果和结算明细。后台提供演示期号、赔率、账户、注单和开奖控制能力；真实用户、代理、支付和生产运营功能不纳入首期。

## 2. 事实基础与范围边界

方案依据：

- `docs/参考资料/汇博盈-业务参考.md` 中的参考页只读观察。
- 参考页公开 HTML 中内嵌的玩法说明和当前赔率展示。
- 当前项目已确定的 Java 21、Spring Boot 4.0.8、MySQL 8.4 LTS、Flyway 技术基线。

首期确认范围：

- 只处理一个游戏类型和一个期号流。
- 每期生成或由后台录入 8 个 01～20 的号码。
- 每个位置独立计算番、单双、大小。
- 自动轮期按当前确认规则运行：每期 300 秒，前 180 秒下注，后 120 秒开奖中；首期期号为 `3000000`。
- 开奖阶段展示 8 个 01～20 的随机预览值，最终开奖再生成 8 个随机值；允许重复，不要求去重。
- 支持番、角、车、严、加、正、通/无、单双、大小、特等玩法的结构化结算。
- 赔率存储为可修改配置，结算时保存下注使用的赔率快照。
- 页面展示当前期、封盘状态、下注列表、开奖结果、赢和输结算明细。

首期明确不做：

- 真实资金、支付、提现、充值、代理分成和第三方开奖接口。
- 真实用户账户、密码、权限后台和生产运营流程。
- 参考页的聊天机器人、跟投、飞单、历史走势算法和多游戏入口。
- 未经确认的总结果聚合规则、和局返还金额和特玩法组合金额口径。

## 3. 结算规则

对单球号码 `n`：

```text
fan = n % 4；余数 0 映射为 4
parity = fan 为 1 或 3 时为单，否则为双
size = n 在 01～10 时为小，在 11～20 时为大
```

玩法判定返回 `WIN`、`DRAW` 或 `LOSE`：

| 玩法 | 结构 | 判定 |
| --- | --- | --- |
| 番 | 一个番数 | 命中为赢，其余为输 |
| 角 | 两个番数 | 任一命中为赢，其余为输 |
| 车 | 三个番数 | 任一命中为赢，未选番为输 |
| 严 | 赢号、和号 | 赢号为赢，和号为和，其余为输 |
| 加 | 赢号、两个和号 | 赢号为赢，两个和号为和，余号为输 |
| 正 | 一个主番 | 主番为赢，相对番为输，另外两个为和 |
| 通/无 | 两个赢号、一个输号 | 赢号为赢，输号为输，剩余番为和 |
| 单双 | 单或双 | 按番的奇偶命中为赢，否则为输 |
| 大小 | 大或小 | 按实际号码 11～20/01～10 命中为赢，否则为输 |
| 特 | 一个或多个实际号码 | 号码直接命中为赢，否则为输 |

赔率结算采用可配置口径：

```text
LOSE: -stake
WIN: stake * (odds - 1) 作为净盈亏
DRAW: drawPolicy 配置，首期默认退回本金，净盈亏为 0
```

页面当前观察到的默认赔率为：番 3.850、角 1.950、念 2.900、单双/大小 1.950、正 1.950、车 1.316、两赢一输一和的无组合 1.950、三赢一输的无组合 1.475、特 18.00。实现中必须保存赔率快照，不能依赖开奖后的当前赔率。

## 4. 技术架构

首期使用单体 Spring Boot 服务，浏览器页面由同一服务的静态资源提供，减少前后端联调成本：

```text
浏览器静态页面
    -> REST API：当前期、轮期状态、玩法赔率、下注、演示后台控制
    -> Spring Boot Controller
    -> 领域服务：期号、玩法解析、结算
    -> Repository/JDBC
    -> MySQL 8.4 LTS；测试使用 H2
```

首期暂不引入 Redis、Quartz、WebSocket、MyBatis 和微服务。自动轮期使用 Spring `@Scheduled` 每秒推进，前台使用 REST 轮询；后续若确认多人实时消息需求，再评估 SSE/WebSocket。

## 5. 数据边界

首期至少需要以下实体：

- `game_issue`：期号、状态、轮期阶段、下注结束时间、开奖结束时间、最终 8 个号码、创建和更新时间。
- `game_issue_event`：轮期开始、封盘提醒、进入开奖和开奖结果等幂等机器人事件。
- `game_odds`：玩法编码、显示名称、赔率、和局策略、启用状态、版本。
- `game_bet`：期号、球位、玩法编码、玩法参数、投注额、赔率快照、结算状态、净盈亏。

金额统一使用 `BigDecimal`，数据库使用 MySQL `DECIMAL`。任何开奖结果只能在期号状态允许时写入；结算必须具备幂等保护，同一期同一注单不能重复结算。

## 6. 验收标准

1. 浏览器打开首页后能够看到当前期、8 个球位、倒计时、玩法赔率和下注区域。
2. 后台演示接口可以设置 8 个号码和当前期状态，前台刷新后显示结果。
3. 输入 `第8球/3番15` 等价的结构化下注后，能够保存下注和赔率快照。
4. 给定固定号码和固定赔率时，番、角、单双、大小、特、严、加、正、通/无、车的 WIN/DRAW/LOSE 结果与方案表一致。
5. 结算结果显示投注额、判定、赔率和净盈亏，并且重复请求不会产生重复结算。
6. `.\mvnw.cmd test` 在 JDK 21 环境通过；至少包含领域结算单元测试、Controller 参数校验测试和 Flyway/H2 上下文测试。
7. 页面在 375px 手机宽度和桌面宽度下均可操作，不出现横向滚动，输入和按钮具有清晰的焦点及禁用状态。

## 7. 风险与确认项

- 参考页的“特”玩法前端展示金额与普通玩法的金额口径不同，必须通过甲方确认或固定测试样例确认。
- 页面当前展示的是动态赔率，不应直接当成永久产品配置。
- 参考页的总结果、聊天和资金行为不属于首期结算核心，不能为了视觉相似而提前引入。
- 本项目仅按软件演示范围实现，真实运营前必须重新进行合规、资金和权限设计。

## 8. 历史方案：聊天室前台与演示后台改造（已被当前需求替代）

> 本节记录 2026-09-10 之前形成的阶段性实现方案。它曾指导 Vue 聊天室式演示页面，但没有达到“按汇博盈页面进行复刻”的当前要求。后续页面工作不得直接按本节实施，应先阅读 [当前需求基线：汇博盈页面复刻](需求基线/2026-09-10-汇博盈页面复刻.md)。

### 8.1 改造目标

首期演示基础已经完成，下一阶段重点改造前端信息架构和演示控制方式：

- 用户前台改为移动端优先的聊天室式界面，复现参考页观察到的期号、球位、消息流和底部输入键盘结构。
- 管理后台独立承载赔率、演示用户余额、中奖号码、封盘开奖和注单核对操作。
- 后端先支持可重复验证的演示闭环，不引入真实充值、提现、支付或生产资金能力。

### 8.2 技术方案

- 前端改为 Vue 3、TypeScript、Vite；一个前端工程内分为用户前台和管理后台两个路由区域。
- 后端继续使用当前 Spring Boot 单体服务和 MySQL 8.4 LTS/Flyway；开发时前后端可分离运行，构建后由 Spring Boot 托管静态资源。
- 首阶段使用 REST 加轮询获取期号、余额、赔率、消息和结算结果；WebSocket 延后到消息实时性需求确认后实施。
- 用户余额定义为后台管理员分配的虚拟演示余额；分配多少就是多少可用额度，余额调整必须落余额流水，不能只改一个余额字段。该余额不代表充值、支付或可提现资金。

### 8.3 用户前台范围

- 顶部展示演示用户、余额、当前期号、倒计时和封盘状态。
- 中部展示第 1 球至第 8 球的球位选择和按球结果。
- 主体展示系统公告、机器人式结果消息、下注确认、开奖和结算消息。
- 底部提供玩法、番数/号码、金额快捷键、下注预览和发送按钮。
- 文本格式只作为展示和快捷输入，不直接让结算服务依赖字符串解析；提交前转换为结构化下注请求。

### 8.4 演示后台范围

- 赔率管理：修改玩法赔率并立即影响新的赔率快照。
- 演示账户：查看用户余额，增加或扣减虚拟余额并记录原因。
- 期号控制：录入 8 个中奖号码、封盘开奖、开启下一期。
- 注单核对：按期号查看下注、赔率快照、WIN/DRAW/LOSE 和净盈亏。

### 8.5 明确不纳入本阶段

- 真实用户认证、角色权限和生产审计体系。
- 充值、提现、支付、代理分成和第三方开奖接口。
- 真实多人聊天、机器人算法和生产级开奖源。
- `特` 的本金口径、和局返还和总结果聚合等尚未确认的业务规则。

## 9. 当前需求入口

用户前台后续开发以 [当前需求基线：汇博盈页面复刻](需求基线/2026-09-10-汇博盈页面复刻.md) 为准。新的实施计划必须基于该基线重新编写，旧的聊天室式页面计划只用于追溯，不得作为当前验收标准。

自动轮期和随机开奖以 [当前需求基线：自动轮期与随机开奖](需求基线/2026-09-10-自动轮期与随机开奖.md) 为准。

## 10. 当前下一阶段入口：聊天室与常驻机器人

原首期演示方案中的“暂不引入 WebSocket、使用 REST 轮询”仍适用于首期玩法和自动轮期演示的已实现范围。下一阶段如果实施正常聊天室和服务端常驻机器人，以 [聊天室与常驻机器人改造方案](plans/2026-09-10-聊天室与常驻机器人改造方案.md) 为准；该方案不把聊天消息混入 `game_issue_event`，也不把机器人展示消息当作真实用户订单或资金行为。

### 10.1 最新确认：虚拟余额而非真实支付

正式用户可以参与前端下注，但使用的是后台管理员分配的虚拟演示余额。当前设计不接入充值、支付、提现、转账、代理分成或真实资金清算；管理员分配、用户下注扣款和开奖结算均必须写入服务端余额流水。

认证阶段和虚拟余额阶段均已完成：Flyway `V8` 已将现有 `demo_user_account` 兼容关联到 `sys_user`，管理员分配、认证用户下注、手工/自动结算和审计已通过 H2 与独立 MySQL 8.4 验证。当前余额是管理员分配的虚拟演示额度，不是充值或可提现资金；聊天室从 `V9` 开始，不能继续使用固定 `DEMO-USER` 作为正式用户身份。

本阶段执行证据记录在 `docs/plans/2026-09-10-虚拟余额钱包与管理员分配开发计划.md`：后端全量 H2 90/90、MySQL 8.4 钱包专项 18/18、前端类型检查和生产构建均通过。

### 10.2 最新执行状态：聊天室 REST 持久化纵向切片

2026-09-11 已完成聊天室阶段 3 的 REST 持久化纵向切片：Flyway `V9`/`V10` 创建房间、消息、outbox、禁言和已读游标表；后端实现服务端发送者归属、房间序号、客户端幂等、纯文本校验、禁言拒绝、历史游标和 REST 权限路由；前端已切换为 REST 历史与短轮询，不再使用固定机器人或浏览器本地假消息。

验证证据：聊天专项 H2 `17/17`、后端全量 H2 `107/107`、独立 MySQL `8.4.11` 从 V1 迁移到 V10 且聊天专项 `17/17`、前端 `npm run typecheck` 和 `npm run build` 均通过。MySQL 验收使用独立临时库，已在验收后删除，未修改现有业务库。

当前边界：本段记录的是 2026-09-11 的阶段快照；后续运行态与发布结果以 10.4 节和验收记录为准。服务端常驻机器人、Redis 多实例、聊天治理后台仍未实现。

### 10.3 当前阶段：单实例 WebSocket 与前端实时消息流

已完成单实例 WebSocket 的 ticket 握手、Origin/权限校验、连接注册、心跳、连接上限、消息限流、订阅同步、消息发送 ACK、服务端持久化后广播、幂等复用、已读游标和前端重连/REST 降级接入。前端 Vite 已配置 `/ws` 代理，Spring Boot 静态资源已重新构建。

### 10.4 2026-09-13 运行态与发布收尾结果

- 已验证：独立 MySQL 8.4.11 验收库 V1-V10、Spring Boot 真实启动、登录/聊天室 REST、一次性 ticket、本地 WebSocket 双连接、消息序列、幂等去重、账号停用后的既有连接关闭、Nginx Upgrade、systemd 重启和受控回滚。
- 已验证：两个隔离 Playwright 浏览器上下文在 375px 和桌面宽度进入 `/room`，实时消息在两个页面各显示一次且无横向溢出。
- 仍需补充：页面刷新恢复、浏览器断网重连和服务器页面 DOM 回归；这些未完成前不能表述为生产高可用或完全结项。
- 下一阶段入口仍为服务端常驻机器人基础能力：机器人使用独立身份和 `ROBOT` 消息类型，消息必须持久化、可重试、可审计，不能创建注单、扣减虚拟余额或伪造普通用户发言。

自动化证据：后端全量 H2 `135/135`、聊天室与权限专项 `34/34`、前端 Vitest `11/11`、`npm run typecheck` 和 `npm run build` 均通过。独立 MySQL 8.4.11 运行态、两个 Playwright 隔离上下文、服务器 Nginx Upgrade、systemd 重启和受控回滚已验证；页面刷新恢复、浏览器断网重连和服务器页面 DOM 回归仍待补齐。该阶段仍是单实例 MVP，不代表 Redis 多实例高可用。

### 10.5 当前前置补齐：用户管理最小可用后台

用户管理最小后台代码切片已完成：后端提供管理员初始化、分页/关键字筛选、详情、创建普通用户、状态、密码、角色和会话失效接口；前端提供 `/login`、`/admin/users` 和 `/forbidden`，增加受保护路由、刷新会话、401 分类和退出登录；新用户固定获得 `USER` 角色和 `0.00` 虚拟钱包；管理员余额分配仍走现有钱包接口和流水。实现参考若依的 Controller 拆分、Service 事务、权限标识和操作日志边界，但没有复制若依表结构、依赖或默认数据。

验证已完成：用户管理历史切片的后端全量 H2 `117/117`、用户管理专项 `29/29`、前端类型检查和构建均通过；其运行态待补齐项已在本次独立 MySQL 与 Playwright 验收中覆盖，当前遗留项以 `docs/验收记录/2026-09-13-单实例聊天室运行态验收记录.md` 为准。详细边界见 [用户管理最小可用后台开发计划](plans/2026-09-11-用户管理最小可用后台开发计划.md)。

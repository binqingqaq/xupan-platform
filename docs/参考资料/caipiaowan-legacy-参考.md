# caipiaowan 参考源码

## 资料属性

- 类型：相似项目源码参考
- 本地路径：`D:\codes\liuhecai\_caipiaowan_extract\caipiaowan-main`
- 获取形态：解压源码目录
- Git 状态：当前目录没有 `.git`，没有可复用的提交历史
- 使用边界：只读分析和业务建模参考，不作为当前项目依赖、代码来源或需求事实

## 静态观察结果

从目录和源码可以确认该副本包含：

- PHP 应用代码和 ThinkPHP 风格的模块结构。
- 前台、后台和代理相关模块。
- 用户、订单、开奖/期号、配置和机器人相关控制器或数据访问代码。
- `Workerman/` 目录，以及多个实时开奖/游戏 WebSocket 控制器。
- 多游戏入口和实时消息、历史记录、投注/结算相关实现痕迹。

## 文档冲突和验证边界

- `README.md` 声称是 ThinkPHP 8.1.3 迁移版，并写有 PHP 8.0+、MySQL 和多游戏 WebSocket 说明。
- `DEPLOY.md` 又写成 ThinkPHP 3.2.3、SQLite 和另一套目录/启动方式。
- 当前目录没有 `.sql` 文件；README 中提到的数据库文件不能仅凭文档视为已存在。
- 没有在当前分析中完成 PHP、MySQL、WebSocket、外部接口或完整业务流程的运行验证。

因此，后续只能从实际源码、配置和测试结果提取可复用信息，不能直接复制参考仓库文档中的版本、数据库或部署结论。

## 可参考方向

- 期号、开奖状态和实时消息的业务拆分方式。
- 用户端、后台端和代理端的模块边界。
- 订单、用户余额、开奖结果和机器人消息之间的关联关系。
- WebSocket 实时推送与后台任务的分离思路。
- 多游戏配置和不同游戏规则的组织方式。

## 禁止直接复用

- 参考仓库中的密码、Token、支付参数、真实用户数据和外部接口凭据。
- 未经当前项目需求确认的投注、支付、提现、代理、结算和机器人规则。
- 参考仓库的 PHP/ThinkPHP 版本结论、数据库初始化结论和部署命令。
- 未经许可证和安全审查的代码、图片、文案及数据。

## 与当前项目的关系

当前项目使用 Java 21、Spring Boot 4.0.8、Spring MVC、MySQL 8.4 LTS 和 Flyway。参考仓库是业务理解材料，不能要求当前项目照搬 PHP/ThinkPHP/Workerman 技术栈；是否需要实时通信、机器人模块、订单模块和后台模块，必须在当前项目方案中分别确认。

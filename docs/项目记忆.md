# xupan-platform 项目记忆

> **当前阅读门禁（2026-09-23）：** 本文是跨窗口交接和历史决策记录，不是当前功能规格。新开发先阅读 [文档索引](文档索引.md) 及其业务模块基线，再使用本文补充历史背景；后文旧阶段记录不能覆盖当前源码、测试和最新验收证据。

> 本文件是仓库内的项目交接记录，用于记录当前项目阶段、已验证事实、关键决策和下一步。它不是密码、Token、数据库凭据或真实业务敏感数据的存储位置。

## 项目交付定位（用户已确认）

- 本项目是面向服务器正式上线、持续运行和交付使用的产品，不是一次性演示代码。
- 业务能力可以按阶段交付，但每个阶段的新设计都必须同时考虑数据持久化、Flyway 迁移、备份恢复、数据一致性、认证授权、敏感信息保护、操作审计、并发幂等、故障恢复、日志监控和发布运维。
- 演示数据、fixture、前端临时状态和内存缓存必须隔离标注，不能成为生产事实源；未实现的生产能力必须写成未实现或未验证，不能由本机测试推断为服务器或生产结论。
- 后续发现仅适用于演示的旧设计时，应优先补齐生产边界或明确替换计划，不继续扩大其在正式链路中的使用范围。

2026-09-18 用户再次确认上述产品定位，后续所有开发和设计评审均以此为前置约束。

## 记录信息

- 记录日期：2026-09-18
- 项目根目录：`D:\codes\liuhecai\xupan-platform`
- 当前 Git 分支：`main`
- 当前代码基线已包含 Vue 用户前台、演示/用户管理后台、自动轮期演示闭环、单实例聊天室实时链路和服务端常驻机器人基础能力；后续以当前源代码与最新测试结果为准。

## 技术基线

- Java：`21`。
- Spring Boot：`4.0.8`。
- 生产数据库：MySQL `8.4 LTS`，已由用户于 2026-09-08 确认。
- 数据库迁移：Flyway。
- 测试数据库：测试 Profile 使用内存 H2，不代表生产数据库验证。
- 选择 MySQL `8.4 LTS` 是为了使用当前 LTS 系列；不能只写“MySQL 8”，也不能默认回退到 8.0。

MySQL 官方资料：

- [MySQL 8.4 LTS release model](https://dev.mysql.com/doc/refman/8.4/en/mysql-releases.html)
- [MySQL 8.0 release notes](https://dev.mysql.com/doc/relnotes/mysql/8.0/en/)

## 当前阶段

项目已完成首期玩法演示、Vue 用户前台/后台、汇博盈页面复刻的首轮代码实施、自动轮期演示闭环、用户认证与管理、单实例聊天室、服务端常驻机器人基础闭环和机器人管理前端代码切片。当前前台可使用 `/room`，正式管理控制台为 `/console`，机器人管理页面为 `/console/robots`，旧 `/admin/*` 自动跳转；机器人管理管理员 API 和事件级运行态专项已完成本机 MySQL 验收，浏览器视觉检查、正式运维演练、公网 HTTPS 刷新恢复和生产运营能力仍需后续完成或确认。

已确认的工程结构：

```text
xupan-platform/
├── AGENTS.md
├── README.md
├── docs/
└── server/
    └── xupan-server/
```

## 已验证事实

- 根目录 Git 仓库已建立。
- 初始工程提交为 `5c3e0bc chore: initialize xupan platform project`。
- H2 测试隔离提交为 `4d3403d test: isolate context test with h2`。
- 后端模块位于 `server/xupan-server`。
- `pom.xml` 使用 Spring Boot `4.0.8`，Java 版本为 `21`。
- 生产依赖包含 MySQL Connector、Flyway MySQL 和 JDBC；本机 MySQL `8.4.11`、服务 `MySQL84`、端口 `3307` 已通过 Spring Boot 真实启动验证。
- 测试依赖包含 H2；测试类使用 `test` Profile。
- 历史工程初始化阶段在 JDK 21 环境下执行 `server/xupan-server/.\mvnw.cmd test` 已通过 13 个测试；截至 2026-09-14，最新后端全量测试为 `195/195`，0 个失败，0 个错误。
- 2026-09-09 已验证腾讯云 Ubuntu 服务器的 JDK 21 安装路径为 `/www/server/java/jdk-21.0.2`；系统登录环境配置位于 `/etc/profile.d/xupan-java21.sh`，后续 systemd 服务复用的环境文件位于 `/etc/xupan-platform/java21.env`。
- 2026-09-09 通过服务器实际命令验证 `java -version` 输出为 `21.0.2 LTS`；项目 Jar 已部署到 `/opt/xupan-platform/releases/`，由 `xupan-server.service` 管理，Nginx 通过 80 端口反向代理到本机 8080。
- `target/` 和 IDEA 配置已被根目录 `.gitignore` 忽略。
- 若依参考仓库已浅克隆到 `D:\\codes\\liuhecai\\_ruoyi-reference\\RuoYi-Vue`，当前参考提交为 `13db1fc`；它只作为模块和架构参考，不并入当前仓库。
- 相似业务参考源码位于 `D:\\codes\\liuhecai\\_caipiaowan_extract\\caipiaowan-main`，只读参考。
- 参考网页只读观察到 8 个位置、每个位置 01～20 号码，以及逐球 `番/大小/单双` 结果展示；其中 `番` 与号码除以 4 的余数一致，余 0 按 4 番处理，`单双`按番判断，`大小`按实际号码 01～10/11～20 判断。
- 参考页内嵌玩法说明已确认 `番/角/车/严/加/正/通/无/单双/大小/特` 的基本判定和文本格式；页面当前赔率已记录在 `docs/参考资料/汇博盈-业务参考.md`，但赔率口径和特殊玩法结算金额仍待甲方确认。
- 已实现 8 球领域结果、玩法结算、Flyway 表结构、JDBC 仓储、演示 API 和 Vue 静态构建页面；真实 API 已完成一次 MySQL 冒烟：下注、赔率快照、开奖、中奖盈亏、重复开奖拒绝和封盘下注拒绝。
- 2026-09-10 重启本地服务后已验证 MySQL 8.4 应用账号连接成功，Flyway V3/V4 迁移成功，`/api/demo/game/current` 返回 `3000000`、`BETTING`、下注/开奖截止时间和轮期事件。

## 当前实现范围

已实现：

- Spring Boot 启动类 `XupanServerApplication`。
- 基础 Spring Boot 上下文测试。
- 基础 Actuator、Security、Validation、WebMVC、Flyway 相关依赖。
- 测试专用内存 H2 数据源配置。
- 业务参考记录：`docs/参考资料/汇博盈-业务参考.md`，包含当前网页观察和待确认规则边界。
- 8 球番摊领域模型和结算服务，金额使用 `BigDecimal`，赔率最多三位小数。
- Flyway V1～V4 迁移，包含轮期阶段字段、`game_issue_event` 以及 `game_issue`、`game_odds`、`game_bet` 三张业务表。
- JDBC 仓储、赔率快照和 `settled_at IS NULL` 幂等结算条件。
- `/api/demo/game/current`、`/api/demo/game/bets`、`/api/demo/game/admin/draw`、`/api/demo/game/admin/odds/{playType}` 和本地演示用 `/api/demo/game/admin/reset`。
- 首期原生演示页面已由 `web/` 下的 Vue 3 + TypeScript + Vite 用户前台和演示后台替代，构建后由 Spring Boot 托管。
- 2026-09-10 已完成参考页的桌面/窄屏只读视觉审计，确认存在聊天界面、界面2五列赔率/快捷下注界面、历史面板、设置弹层、玩法说明和搓牌开奖入口；这些观察记录在当前会话和复刻实施计划中，不把参考站点地址或私有资源写入仓库。

## 2026-09-10 上一阶段聊天室前台与演示后台改造（历史）

> 本节记录上一阶段实现，不代表当前页面复刻需求已经完成。

- 已新增 `web/` Vue 3 + TypeScript + Vite 前端工程，用户前台为 `/room`，演示后台为 `/admin`。
- 用户前台已改为移动端优先的聊天室式布局，包含期号、8 个球位、消息流、玩法键盘、金额快捷键和下注预览。
- 后台已提供赔率、演示余额、余额流水、开奖控制和注单核对页面；当前使用 REST 轮询，不包含 WebSocket。
- Flyway V2 已新增 `demo_user_account`、`demo_balance_ledger`，并将 `game_bet` 关联演示用户；下注扣款、开奖派彩和余额调整均写入流水。
- 已验证前后台闭环：修改赔率、调整余额、下注扣款、录入 8 个号码、WIN 结算和前台消息展示。
- `mvnw.cmd test` 当前通过 13 个测试；`npm run typecheck` 和 `npm run build` 当前通过。
- 本节只记录上一阶段实现过程；自动轮期、当前页面状态和最新验收结果以本文后面的当前记录及项目方案为准。

## 2026-09-10 自动轮期与随机开奖

- 用户已确认：8 个球号分别在 01～20 内随机生成，允许重复，不做去重；首期期号从 `3000000` 开始。
- 每期固定 300 秒：前 180 秒为 `BETTING`，到时自动封盘并进入 `DRAWING`；后 120 秒开奖中，期满生成最终号码、结算待结注单并自动创建下一期。
- 服务端使用 Spring `@Scheduled(fixedDelay = 1000)` 推进状态，前台通过 `/api/demo/game/current` 每秒轮询；服务端返回阶段、服务端时间、两个阶段截止时间和轮期事件。
- 随机开奖使用 `SecureRandom` 生成最终 8 个号码；开奖中的滚动预览使用按期号和秒数稳定的伪随机值，避免同一秒不同客户端显示不同结果。
- Flyway 新增 V3 轮期字段和 V4 轮期事件表；自动轮期状态转换、最终开奖和事件写入均有幂等条件。
- 已验证：`自动轮期服务Test` 能从 `3000000` 推进到开奖中，期满将旧期置为 `SETTLED` 并创建 `3000001`；前端 `npm run typecheck`、`npm run build` 已通过。

## 2026-09-10 当前需求基线调整（已完成首轮实施）

- 用户已明确：用户前台需要按汇博盈真实网页进行视觉、结构和交互复刻，不接受仅保留业务元素的自定义聊天室页面。
- 已新增 `docs/需求基线/2026-09-10-汇博盈页面复刻.md`，作为汇博盈目标的用户前台需求入口；该目标与后续新增的 168开奖网首页展示端目标并行。
- 原“聊天室前台与演示后台改造”以及“聊天室与常驻机器人改造方案”已经被当前业务模块基线替代，旧计划已删除；当前页面判断只看文档索引、展示端/聊天/机器人模块基线、源码和验收记录。
- 已新增 `docs/plans/2026-09-10-汇博盈页面复刻实施.md`，作为汇博盈目标的代码实施和验收清单；168开奖网首页使用独立的日期基线和实施计划。
- 已将用户前台调整为参考页风格的固定顶部、开奖条、消息流、输入区、数字键盘和搓牌开奖层，并补充界面2五列赔率视图、底部快捷下注栏、历史面板和设置弹层。
- 已在本地 Vite 页面验证：界面2切换成功、赔率卡片与快捷栏可见、历史显示 8 条记录、设置弹层含 5 个金额输入、返回聊天可用；`npm run typecheck` 和 `npm run build` 通过。
- 2026-09-10 已在 5173 和 8080 页面观察到 `3000000`、下注倒计时和机器人开期消息；正式 375px/桌面截图差异清单仍未完成。

## 2026-09-10 用户认证与虚拟余额钱包阶段

- 用户认证与权限基础已完成：Flyway `V1` 至 `V7`、登录/刷新/退出、可撤销会话、权限默认拒绝、WebSocket 票据和登录/操作审计均已纳入当前实现。
- 虚拟余额钱包已完成：Flyway `V8__建立正式用户虚拟钱包.sql` 建立 `sys_user` 与 `demo_user_account` 一对一关联、流水幂等字段、注单关联和钱包权限种子；历史 `DEMO-USER` 保留为兼容数据。
- 管理员分配多少，成员虚拟钱包就增加多少；管理员调整、下注扣款、手工开奖结算和自动轮期结算均由服务端写不可变流水并受权限、事务和幂等键保护。
- 当前余额不是支付系统，不支持充值、提现、转账、代理分成、第三方支付回调或真实资金清算。
- 验证证据：后端全量 H2 `90/90` 通过；独立 MySQL `8.4.11` 验收库从 V1 迁移到 V8 成功，钱包专项 `18/18` 通过；前端 `npm run typecheck` 与 `npm run build` 通过。

## 2026-09-11 聊天 REST 持久化纵向切片

- 已完成 Flyway `V9`/`V10`：`chat_room`、`chat_message`、`chat_outbox`、`chat_user_mute` 和 `chat_read_cursor`，首期公开大厅种子为 `main`。
- 已完成普通用户聊天 REST 闭环：房间摘要、历史 before/after 游标、服务端身份归属、房间序号、客户端幂等、纯文本校验、禁言拒绝、已读游标和消息/outbox 同事务写入。
- `UserRoom.vue` 已改为 REST 历史加短轮询，访问令牌改为模块内存变量；固定参考用户、固定机器人和本地假消息不再作为聊天事实来源。
- 验证证据：聊天专项 H2 `17/17`、后端全量 H2 `107/107`、独立 MySQL `8.4.11` V1-V10 迁移与聊天专项 `17/17`、前端类型检查和生产构建全部通过；临时 MySQL 验收库已删除。
- 边界：MockMvc 已覆盖双用户和权限/幂等场景，但真实浏览器双用户手工联调未执行；WebSocket、常驻机器人、Redis 多实例和治理后台仍未实现。

## 2026-09-11 用户管理后台登录闭环

- 已补齐前端 `/login` 和 `/forbidden` 页面、受保护路由守卫、会话启动恢复、401 错误分类、管理后台退出登录和原目标地址回跳。
- 访问令牌仍只保存在前端内存；刷新使用 HttpOnly Cookie。本地 HTTP 验收通过 `XUPAN_AUTH_REFRESH_COOKIE_SECURE=false`，生产 HTTPS 必须由外部环境变量开启 Secure。
- 本机 MySQL 已验证管理员登录、刷新 Cookie、刷新后新访问令牌、`/api/auth/me` 和管理员用户列表；后端全量测试 `117/117`，前端类型检查和生产构建通过。
- 当前仍待两个普通用户创建、虚拟余额分配、双浏览器聊天室人工验收和独立 MySQL 8.4 回归；WebSocket 代码已接入但尚未完成真实运行态验收，常驻机器人、Redis 多实例和治理后台仍未实现。

## 2026-09-13 单实例聊天室运行态与发布收尾

- 当前发布候选提交为 `69a3a67`；本次补验在其基础上增加浏览器离线/恢复处理，后端全量 H2 `135/135`、聊天室与权限专项 `34/34`、前端 Vitest `12/12`、typecheck 和 build 均通过。
- 独立 MySQL 8.4.11 验收库 `xupan_platform_ws_accept_20260913` 已完成 V1-V10，真实 Spring Boot 健康端点、登录、聊天室 REST、一次性 ticket 和本地 WebSocket 双连接均验证成功；验收账号和密码未写入文档。
- 前端使用两个隔离 Playwright 上下文完成本地 `/room` 页面验收：375px 与桌面宽度均无横向溢出；刷新后保持会话并恢复历史，浏览器离线期间不提前收消息，恢复网络后按游标补齐且不重复。
- 服务器已发布当前 Jar，systemd 重启后健康 200；Nginx `/ws/` Upgrade 配置、公共链路双协议会话、临时移除 `/ws/` 的失败演练和恢复均已验证。服务器 Nginx 由面板进程管理，实际使用 `nginx -s reload`，不是 `systemctl reload nginx`。
- 本次补充了既有 WebSocket 连接的会话/用户状态/聊天室读权限复核：失效连接关闭码为 4401 或 4403；读游标接口也要求 `PERM_CHAT_ROOM_READ`。
- 公网服务器的 375px/桌面关键 DOM 回归已通过；公网当前为 HTTP，Secure 刷新 Cookie 未下发，公网刷新恢复需启用 HTTPS 后复验。
- 当前仍不具备：多实例广播、Redis/MQ 和聊天治理后台；常驻机器人服务端基础能力已完成，真实支付和生产高可用仍未实现。

## 2026-09-14 服务端常驻机器人基础能力

- 已新增 Flyway `V11`、`V12`：`chat_robot`、`chat_robot_template`、`chat_robot_dispatch`；默认机器人为 `issue-helper`/`开奖助手`，不属于 `sys_user`，不拥有钱包、下注或登录权限。
- 已完成服务端调度闭环：扫描已提交的 `game_issue_event`，覆盖 `ISSUE_STARTED`、`BETTING_WARNING`、`BETTING_CLOSED`、`DRAW_RESULT`；使用稳定机器人选择、模板版本、任务租约、失败退避、唯一 dispatch 和消息幂等键。
- 机器人消息复用聊天室 `chat_message`、房间序号、`chat_outbox` 和提交后 WebSocket 广播；无在线连接时仍持久化，重复扫描不会重复消息，不调用用户权限、下注或钱包服务。
- 已提供机器人管理 API：查询、创建、编辑、启停、模板查询/更新/预览、投递记录和失败重试；服务层权限为 `ROBOT_READ`、`ROBOT_WRITE`、`ROBOT_TEMPLATE_WRITE`，配置和重试动作写操作审计。
- 验证结果：后端全量 H2 `180/180` 通过；MySQL `8.4.11` 单服务器运行态已验证事件发布、无在线持久化、重复扫描唯一性、停用/模板缺失跳过、人工失败重试、停机后 `PENDING` 恢复和资金隔离。凭据未写入文档。
- 当前边界：当前部署目标就是单服务器、单实例；机器人管理前端代码、管理员 API、事件级运行态验收和管理员登录后的页面冒烟已完成，精确 375px/桌面视觉检查按用户确认暂缓，无 `ROBOT_READ` 账号的 `/forbidden` 自动跳转、历史批量补发、归档清理和正式运维告警仍未完成。Redis/MQ、多实例任务协调和跨节点广播不属于当前开发或正式上线前置条件，正式上线后根据真实运行数据再评估。验收详情见 `docs/验收记录/2026-09-14-服务端常驻机器人基础能力验收记录.md` 和 `docs/验收记录/2026-09-14-机器人管理前端与单服务器运行态验收记录.md`。

仍未实现或尚未确认：

- 公网 HTTPS、Secure 刷新 Cookie 和正式域名下的刷新恢复；本地单实例刷新、离线重连和服务器页面关键 DOM 已完成验证。
- 聊天治理后台和正式运维能力；机器人管理浏览器视觉验收和 `/forbidden` 页面检查尚未完成。
- 正式生产部署配置、备份恢复、故障演练和生产运营规则。
- 真实资金、第三方开奖源、特玩法本金口径、和局返还和总结果聚合。

## 已知边界

- 测试通过只证明测试上下文可以在 H2 环境加载，不证明 MySQL、网络、生产配置或完整业务流程可用。
- 生产 `application.yaml` 使用环境变量注入数据库连接；仓库不包含密码。
- 当前只具备本地演示能力，不能据此判断已满足真实运营、资金和合规要求。

## 待确认事项

以下内容必须由项目需求确认，不能由代码代理自行假设：

1. 聊天 REST 纵向切片的消息保留、撤回和内容治理规则。
2. 正式上线后的用户量、连接数和故障数据是否达到需要多服务器、Redis/MQ 或跨节点广播的条件；当前单服务器方案不以此为前置确认项。
3. 真实资金、支付、第三方开奖源和生产运营是否进入范围。
4. `特` 玩法的本金口径、和局返还和重复下注规则；限额的按玩家当前期累计口径已于 2026-09-24 确认并实施，多实例部署下直连下注接口的数据库锁或分布式额度控制方式仍待确认。

## 参考资料

- [168开奖网首页参考](参考资料/168开奖网首页参考.md)：新增展示端首页的视觉、结构和移动端参考，与汇博盈业务参考并行。
- [汇博盈业务参考](参考资料/汇博盈-业务参考.md)：用于记录参考网页中观察到的界面和交互，不等同于已确认的产品需求。
- [caipiaowan 参考源码](参考资料/caipiaowan-legacy-参考.md)：用户指定的相似项目源码副本，只读参考，不是当前项目依赖或事实来源。
- [RuoYi-Vue 参考框架](参考资料/RuoYi-Vue-参考.md)：官方若依后端参考仓库，按模块选择性参考，不直接整体复制。

## 下一步建议

1. 配置公网 HTTPS 并在正式域名下复验 Secure 刷新 Cookie、页面刷新和 WebSocket。
2. 完成机器人管理前端浏览器视觉验收，覆盖模板发布、禁用、失败重试、重启恢复和历史消息查看的页面呈现。
3. 完成单服务器部署、备份恢复、日志留存和基础故障演练。
4. 正式上线后根据真实用户量和运行数据评估是否需要多服务器、Redis/MQ、跨节点广播和专门监控告警平台。

## 2026-09-14 168开奖网首页展示端目标

- 用户已明确：后续需要以 `https://16894913.com/` 为参考，复刻首页用于当前项目的公开展示端；移动端参考 `https://m.16894913.com/`。
- 已新增 `docs/参考资料/168开奖网首页参考.md`、`docs/需求基线/2026-09-14-168开奖网首页展示端复刻.md` 和对应实施计划。
- 当前实现方向：展示端页面与 `/room`、`/admin` 保持前端边界，但共用当前 Spring Boot 后端和 MySQL；展示端只使用公开只读查询能力。
- 参考站点只作为视觉、结构和移动端表现参考，不作为当前项目的玩法、赔率、开奖源、支付、接口或后台业务契约。

## 维护规则

- 已验证事实必须能够由源代码、配置、命令输出或测试结果追溯。
- 计划、建议和推断必须使用明确措辞，不能伪装成已实现能力。
- 每次跨窗口继续工作前，先读取本文件、`AGENTS.md`、`README.md` 和当前 Git 状态。
- 不记录密码、Token、真实地址、患者数据、设备敏感数据或其他凭据。

## 2026-09-14 运维可观测性与日志体系

- 已完成请求 requestId 关联、认证和未预期异常日志、生产滚动文件日志、敏感字段脱敏、健康检查和基础业务指标；核心指标覆盖机器人 dispatch、聊天室连接和 outbox pending。
- Flyway V13 新增 SYSTEM_MONITOR_READ，health 可用于存活检查，info 和 metrics 需要该权限；systemd、healthcheck、journald 配置示例和运维故障排查手册已加入仓库。
- 验证结果：后端全量 H2 `195/195`，前端 Vitest `21/21`，typecheck、build、git diff --check 和健康检查脚本 bash -n 均通过。
- 未执行事项：真实服务器 systemd/timer 安装、journald 和日志滚动现场验证、kill/restart 故障演练、外部监控告警联动和公网完整观测链路。相关内容必须保持为待现场验收，不得写成已完成。

## 2026-09-18 聊天室消息容量生产边界

- 聊天室入站 WebSocket 帧默认 `16 KiB`，服务端出站 envelope 默认 `64 KiB`，机器人结构化 `payload_json` 默认 `64 KiB`；三类上限均通过环境变量配置。
- 应用层统一按 UTF-8 字节校验；真实 WebSocket 容器的文本/二进制缓冲覆盖最大线路消息，应用层继续单独拒绝超过入站上限的客户端帧。
- 机器人 payload 超限不截断，进入现有投递失败/重试状态；数据库中的 JSON 仅表示存储类型，不作为无限容量保证。
- 后端全量 H2 测试已通过 `212/212`；服务器真实 WebSocket 大消息、代理缓冲、浏览器和断线重连端到端验证仍待现场执行。

## 2026-09-18 用户消息反馈与下注结算反馈确认

- 用户已确认：`@` 后统一使用用户展示名；用户原文和机器人反馈对聊天室全体用户可见；普通聊天、问候、未确认玩法和错误格式统一回复 `@展示名, 指令格式不正确!`。
- 正确玩法固定下注第 1 球；成功后机器人反馈使用粮草金额和扣款后余额；余额不足直接显示粮草不足；封盘和开奖期间反馈下注无效；开奖后必须反馈结算结果，并同步更新钱包、流水和注单状态。
- “玩法说明、历史、流水”等机器人命令尚未纳入本轮确认；余额不足和结算反馈的最终中文措辞、金额精度仍待后续确认。
- 已完成服务端反馈闭环：`ChatMessageService` 将所有用户输入持久化，并对普通/问候/未确认玩法/错误格式发布 `@展示名, 指令格式不正确!`；确认玩法固定第 1 球下注，成功反馈扣款金额与余额，余额不足、封盘、开奖中发布对应失败反馈。用户原文和机器人消息均为公开聊天室消息，写入 `chat_message`、`chat_outbox` 并复用实时广播。
- 已完成结算反馈接入：`DemoGameService` 和 `自动轮期服务` 在注单成功结算及派彩后发布 `BetSettlementCompletedEvent`，`SettlementFeedbackListener` 在事务提交后以独立事务发布每笔注单的 `ROBOT` 结算消息；消息幂等键为 `CHAT-SETTLEMENT-{betId}-{status}`，反馈失败不会回滚已完成的资金事实。
- 前端 `UserRoom` 已将输入统一交给后端权威处理，保留玩法说明按钮的本地行为，并在下注形态输入后刷新余额、当前期和反馈消息。新增单元/集成覆盖；后端全量 H2 测试 `219/219` 通过。
- 尚未完成的上线验收：独立 MySQL `8.4 LTS`、两个真实用户上下文的全员可见性、真实 WebSocket/代理大消息、服务重启恢复和持久化反馈失败重试队列；这些不能由 H2 或单用户本机页面代替。
- 详细步骤、验收边界和未执行项见 `docs/plans/2026-09-18-用户消息反馈与下注结算闭环实施计划.md`。

## 2026-09-18 透明测试玩家与积分联调

- 测试玩家是明确标记的联调身份，不伪装成真实用户，不提供充值、支付或提现；`sys_user.user_type = TEST`、`demo_user_account.identity_type = TEST`，并禁止通过普通登录入口登录。
- 管理后台新增测试玩家区域，受 `USER_MANAGE` 权限保护，可创建、设置头像、启停、上分和重置积分；余额操作写入流水并记录审计。
- 测试玩家下注固定第 1 球，使用已确认的玩法文本格式，复用正式下注、扣分、封盘拦截、开奖、赔率结算和盈亏统计链路；停用测试玩家不能下注。
- 已执行验证：前端 `typecheck`；前端测试玩家 API/格式测试 4 个测试、29 个断言；后端测试玩家相关 Spring 集成测试 5 个测试全部通过，Flyway V15 在 H2 成功迁移。
- 未执行事项：独立 MySQL 8.4 现场迁移、生产管理员权限配置、服务器部署和真实多用户浏览器验收；不得把本地 H2 结果写成生产验收结论。

## 2026-09-19 统一玩家工作台与托自动行为闭环

- `/admin/test-players` 已统一为玩家工作台：普通玩家和托共用列表、统计、详情、积分操作；左侧总积分/普/托由服务端全量聚合，右侧展示昵称、状态、余额、虚拟积分流水、下注统计和托行为配置。
- 普通玩家创建复用 `UserAdminService.createUser`；托保持 `TEST` 身份并增加 `player_kind=BOT`，现有 `test1`、`test2` 和其他正式用户使用 `player_kind=NORMAL`，不按昵称或用户名猜测分类。
- Flyway V16/V17 新增玩家分类、托行为动作表和 `AUTOMATIC/MANUAL` 模式。自动下注和聊天动作持久化后，统一进入 `ChatMessageService.sendUserMessageWithOutcome`；自动模式按每期调度，手动模式只由“立即执行”触发，动作具有唯一幂等键、状态、租约、重试次数和关联消息/注单。
- 已执行验证：后端 H2/Flyway 全量 226 个测试通过；前端 Vitest 68 个测试通过；`npm run typecheck`、`npm run build`、Java 编译和 `git diff --check` 通过。
- 已执行本机 MySQL 8.4 V17 迁移，并观察自动托在连续两期生成并成功执行下注和聊天动作；未执行真实聊天室双用户观察、实际开奖结算周期、服务器部署、重启演练和生产验收，不得把本机验证写成上线结论。验收记录见 `docs/验收记录/2026-09-19-统一玩家工作台与托自动行为闭环验收记录.md`。

## 2026-09-21 玩家软删除与链接生命周期收口

- 玩家工作台新增事务化软删除：按 `userId` 定位，用户和钱包账户均置为 `DELETED`，安全版本递增，会话和玩家链接撤销，托行为停用；钱包、下注、结算、流水、聊天和审计历史保留。
- 默认玩家列表排除删除状态；管理员显式传 `includeDeleted=true` 才能查询历史。详情返回 wxid、会员 ID、昵称和链接生命周期状态，不返回历史原始 token。
- Flyway V21 已扩展 `demo_user_account.status` 的 `DELETED` 约束；本机 MySQL 8.4.11 已从 V20 迁移到 V21。备份、一致性检查、服务重启和健康检查通过。
- 已执行：阶段三软删除/链接 H2 集成测试、后端专项测试、前端 Vitest 72/72、类型检查、生产构建、本机 MySQL 8.4 迁移和 HTTP 运行态。未执行：服务器/生产发布、托真实开奖周期、本轮删除弹窗 375px 浏览器现场复验、链接元数据定期清理。

## 2026-09-23 上下分审批与最近记录实施

- 用户已确认将统一后台 `/admin` 的两个占位区域替换为“上下分确认”和“最近上下分记录”，顶部当前期号状态保留。
- 玩家或托发送 `上100`、`下100` 等指令只创建 `player_point_request(PENDING)` 并发布聊天反馈，不直接修改余额。
- 管理员同意后二次确认并执行钱包变化、不可变流水和状态审批；取消表示拒绝，不改变余额。
- 待处理申请必须持久化；并发审批只能有一个成功结果。
- 后台提供声音1、声音2和测试提示音，声音偏好保存在管理员浏览器，新申请到达时提示。
- 最近记录默认显示玩家，可切换托，只展示当天实际生效的管理员上分和下调；审批通过与手动上下分进入同一列表。
- 最近记录复用 `demo_balance_ledger`，待审批复用 `player_point_request`，不新增第二套事实源。
- 当天口径默认沿用 `Asia/Shanghai` 每天 06:00 的业务日规则；如需自然日必须后续明确变更。
- 已完成 `/admin` 两个占位区域替换：左侧“上下分确认”支持待处理队列、同意二次确认、取消、两种提示音和测试音；右侧“最近上下分记录”支持玩家/托切换、当天记录和分页。
- 后端新增最近上下分只读接口，只读取 `ADMIN_GRANT` 和 `ADMIN_ADJUST`；待审批列表补齐昵称、会员编号和玩家分类。
- 已验证：后端全量 H2/Flyway `248/248`、前端 Vitest `78/78`、typecheck、build、本机 MySQL Flyway V27、HTTP 接口和浏览器桌面/375px 布局。
- 浏览器已用临时待审批申请验证显示和取消，测试数据已清理；声音2刷新保持、测试提示音无前端错误。
- 未执行：两个管理员真实并发处理同一申请、待审批队列服务重启恢复、平板独立截图、服务器和生产验收。详细证据见 `docs/验收记录/2026-09-23-上下分审批与最近记录验收记录.md`。

## 2026-09-23 后台与玩家链接会话共存修复

- 原问题是管理员和玩家链接共用一个 `xupan_refresh`，新标签页兑换玩家链接会覆盖管理员刷新会话，导致管理员刷新后进入 `/forbidden`。
- 修复后管理员继续使用 `xupan_refresh`，玩家链接使用独立的 `xupan_player_refresh`。
- 刷新接口通过 `X-Xupan-Auth-Audience` 选择 Cookie，并校验刷新会话必须分别是 `PASSWORD` 或 `PLAYER_LINK`。
- 前端每个标签页只在 `sessionStorage` 保存 `ADMIN/PLAYER` 使用端提示，不保存 Token；`/admin*` 强制使用管理员使用端。
- 管理员退出只清管理员 Cookie，玩家退出只清玩家 Cookie。
- 已验证后端全量 H2/Flyway `248/248`、前端 `78/78`、typecheck、build，以及真实管理员标签页与玩家标签页同时登录、分别刷新保持身份。
- 修复前已经被玩家会话覆盖的旧 `xupan_refresh` 会按管理员刷新失败处理，需要管理员重新登录一次。服务器和生产环境仍未验收。

## 2026-09-23 后台正式路径与页面品牌

- 前端后台正式入口由 `/admin` 调整为 `/console`，旧 `/admin/*` 地址保留自动跳转兼容。
- 后台路由统一标题为“AI模型房间管理”，玩家房间和链接入口标题为“奥巴AI”。
- favicon 使用 `web/public/display/picture/favicon.ico`，构建后复制到 Spring Boot 静态资源。
- 前端测试、类型检查、生产构建和后端编译通过；服务器和生产路径及品牌资源未验收。

## 2026-09-23 下注榜只读视图

- 用户已确认在 `/console` 左侧导航下方增加下注榜，包含 `普`、`托`、`上期`、`开奖` 四个页签。
- `普`/`托` 展示当前期数量和总积分，并按 `demo_user_account.player_kind` 区分普通玩家与托；`CANCELED` 注单不计入统计和列表。
- 当前期下注项展示昵称、重建玩法文本和积分；`×` 保留显示但禁用，当前不实现管理员撤单。
- `上期` 当前为空白；`开奖` 展示最近 18 个已结算期号、8 个位置番值和“已开”状态，倒计时以服务端 `serverNow` 与 `phaseEndsAt` 校准。
- 只读接口为 `GET /api/admin/player-desk/bet-board?kind=NORMAL|BOT&limit=500`，继续使用 `USER_MANAGE` 权限；不新增表或 Flyway 迁移。
- 已验证后端全量 H2/Flyway `250/250`、前端 Vitest `85/85`、typecheck、生产构建、本机 MySQL 接口和 `/console` 桌面/375px 浏览器检查；`开奖` 实际渲染 18 行，375px 无页面级横向溢出。
- `×` 由组件级测试确认存在、显示正确且真实禁用；撤单、上期榜单、服务器和生产验收未执行，详见 `docs/验收记录/2026-09-23-下注榜验收记录.md`。

## 2026-09-23 玩家快捷下注面板与玩家偏好

- 玩家页右上角“快捷”已替换为参考式完整面板，旧下拉不再渲染，但旧代码保留。
- 面板包含 74 个赔率项、参考式主赔率拼图、下方赔率网格和底部 170px 快捷下注栏；赔率读取当前服务端 `game_odds`。
- 赔率项支持多选和取消；数字键追加金额，快捷金额替换金额，底部 `X` 回退一位，`重置`只清空赔率选择。
- Flyway V28 新增 `player_quick_bet_preference`，通过 `GET/PUT /api/me/quick-bet-preferences` 按玩家保存五个快捷金额；同一玩家的所有链接共享。
- 快速下注按固定顺序生成逗号分隔文本并进入现有聊天室事务链路；任一注单失败时整批不生效。
- 文字解析新增 `1车`到`4车`和 `2无3`等短格式`无`：`1车`覆盖另外三个番值，`2无3`为 `2` 中、`3` 输、其余番值和。
- 已验证后端全量 H2/Flyway `252/252`、前端 Vitest `89/89`、typecheck、生产构建、本机 MySQL V27 到 V28、桌面/375px 浏览器交互和玩家偏好刷新恢复。
- 后续优化：快捷面板支持再次点击收起；`界面`选择框显示在赔率面板上方并给当前界面打勾；点击`账户流水`会关闭赔率面板并打开流水；快捷面板收起再打开保留滚动位置；刷新统一回普通下注首页；设置保存后的金额更新底部快捷金额按钮，不影响数字键盘；设置弹窗已移除头像和文字提示。
- 补充优化：设置弹窗新增`默认`按钮；账户流水已结算列表只显示当天；账户流水按钮移除悬停文字；机器人文本消息气泡按正文长度自适应。

## 2026-09-24 聊天室四小时历史与新消息提示

- 玩家大厅初始加载最近消息，向上滚动时按`beforeSequence`继续分页，只展示服务端时间最近4小时消息。
- 达到4小时边界后停止加载，并在消息顶部显示`已显示最近4小时消息`。
- 加载更早消息时保持当前阅读位置，避免消息列表跳动。
- 用户不在底部时，新消息只累计未读数量，并显示右下角`▼ N条新消息`浮动提示。
- 点击浮动提示后回到最新聊天节点并清空未读数。
- 已通过前端单元测试、类型检查、生产构建和浏览器双会话验证；服务器、生产和超大消息量场景仍未执行。

## 2026-09-24 后台报网控制静态区域

- `/console` 顶部当前期状态下方新增报网控制静态区域，位于“上下分确认”和“最近上下分记录”上方。
- 视觉包含报网类型、设置账号、退出、登录、押注、网盘余额、移下、报网状态、失败自动退单、自动上报网盘、超时订单和刷新。
- 当前只是静态占位，没有接口、状态、按钮事件或业务逻辑，不改变现有后台和钱包流程。

## 2026-09-24 后台汇盈博三区静态布局

- `/console` 已移除顶部“后台功能导航”块，左侧只保留下注榜、模块预留和兼容入口。
- 后台内容区新增顶部欢迎栏、状态赔率栏和底部线路域名栏，视觉参考汇盈博后台。
- 欢迎栏包含欢迎人、托数量、过期时间、剩余积分、修改密码；状态赔率栏包含当前状态、赔率、限额、特码赔率和特码返水；底部栏包含线路域名、开放注册和登录链接。
- 三部分当前均为静态视觉占位，没有接口、状态或点击逻辑。
- 未执行真实全量 74 项页面下注、服务器和生产验收，详见 `docs/验收记录/2026-09-23-玩家快捷下注面板与玩家偏好验收记录.md`。

## 2026-09-24 后台赔率限额配置与下注校验

- 用户已确认：顶部`赔率`和`特码返水`只保存和展示，不参与赔率或积分计算；`特码赔率`保存后作为特玩法实际赔率。
- 顶部输入不再设置确认按钮，`change`、回车或失焦时自动保存；`限额`改为文本按钮，点击弹出 13 项限额配置窗口，字段不能为空且必须是整数。
- 限额按单个玩家、当前期累计计算，取消注单不占额度；`玩家最高注额`按当前期累计最高额度处理，`玩家最小注额`按每一笔单注最低金额处理。
- 超限或低于最低注额时只拒绝对应那一项，同一组合的其他合法项继续进入正式钱包、注单和聊天反馈链路；机器人反馈包含原因、上限和剩余可下额度。
- Flyway V30 新增单行配置表 `game_betting_config`，保存展示字段和 13 项限额并带 `CHECK` 约束；`特码赔率`写入 `game_odds` 的 `SPECIAL` 行。`通限额`同时覆盖`通`和`无`。
- 接口为 `GET /api/admin/player-desk/betting-config`、`PUT /api/admin/player-desk/betting-config/display` 和 `PUT /api/admin/player-desk/betting-config/limits`，继续使用 `USER_MANAGE` 权限。
- 组合下注的限额拒绝使用 `BetLimitExceededException` 并声明不参与事务回滚，避免把同一批次其他合法项整体回滚；封盘、开奖中、余额不足和玩法参数非法仍按整批拒绝处理。
- 并发口径：聊天室路径（玩家与托的正式下注入口）的聊天室事务对 `chat_room` 取 `FOR UPDATE` 行锁并持有到提交，同一大厅消息按提交顺序串行，限额判断不会读到未提交旧值，该保护来自数据库行锁、不依赖单实例；直连下注接口没有房间行锁，由实例内 `PlayerBetLock` 按玩家串行并覆盖到提交。
- 已验证后端全量 H2/Flyway `272/272`、前端 Vitest `99/99`、`typecheck`、本机 MySQL V29 到 V30 迁移、配置接口 200、同一玩家并发下注不突破额度，以及 `/console` 顶部自动保存和限额弹窗保存。
- 未执行：多实例部署下直连下注接口的数据库锁或分布式锁、真实全量 74 项超限演练、375px 限额弹窗专项、服务器和生产验收。详见 `docs/验收记录/2026-09-24-后台赔率限额配置与下注校验验收记录.md`。

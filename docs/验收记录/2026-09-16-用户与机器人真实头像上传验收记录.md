# 用户与机器人真实头像上传验收记录

## 验收范围

验证用户和机器人真实头像从前端选择文件、服务端 multipart 上传、本地保存、数据库头像键更新，到聊天室历史/实时消息展示的完整链路。

## 代码验证

- 后端 Maven 全量测试：`195/195`，失败 `0`，错误 `0`。
- 前端 Vitest：`24/24`。
- 前端 `npm run typecheck`：通过。
- 前端 `npm run build`：通过。
- `git diff --check`：无代码格式错误。

## 本地运行态验收

本次使用项目 `test` 类路径和临时 H2 数据库在本机 8080 运行，头像目录为系统临时目录，未修改本机 MySQL 业务库。

- `/actuator/health`：`UP`。
- `http://127.0.0.1:5173/room`：HTTP `200`。
- 临时管理员登录：成功。
- 当前用户头像上传：成功，返回随机 `.png` 头像键。
- 管理员用户头像上传：成功。
- 管理员机器人头像上传：成功。
- 头像公开读取：HTTP `200`，响应类型 `image/png`。
- 聊天消息响应包含 `avatarKey`：通过。
- 未登录上传：HTTP `401`。
- 非图片类型上传：HTTP `400`。

## 未执行项

本次未使用本机 MySQL 业务库运行态验收，因为当前终端没有可用的本地数据库密码；不猜测或输出数据库凭据。代码的 H2 测试和隔离运行态已覆盖接口行为，接入 MySQL 前仍应使用本地密钥启动后再做一次真实库冒烟。

# 单实例聊天室运行态验收记录

## 记录信息

| 项目 | 结果 |
| --- | --- |
| 验收日期 | 2026-09-13 |
| 验收范围 | 单实例 Spring Boot、MySQL 8.4、WebSocket、前端实时消息流、Nginx/systemd 发布回滚 |
| 当前代码提交 | `69a3a67b9210baa192eadd8bd718534ecc7bd724` |
| 远程 main 提交 | 计划基线检查时为 `a55e02d`；本次发布候选提交未执行远程 Git 推送 |
| 验收执行人 | Codex 主 Agent 统筹，多 Agent 分工审查 |
| 凭据处理 | 仅使用受控环境变量或本地密钥文件；本文不记录密码、Token、Cookie、ticket |

## 结论状态

当前状态：代码、MySQL 运行态、协议级实时链路和服务器发布回滚已验证；页面刷新恢复及服务器页面 DOM 回归仍未验证。

本记录只承载已经实际执行的命令、接口、日志和浏览器证据。未执行项目保留“未验证”，不以代码存在或计划完成替代运行态证据。

## 环境基线

| 检查项 | 结果 | 证据/备注 |
| --- | --- | --- |
| Java | 已通过 | 本机 21.0.12.1 |
| Node/npm | 已通过 | Node 24.14.0、npm 11.9.0 |
| MySQL | 已通过 | 本机 MySQL Community Server 8.4.11 |
| 本机 MySQL 服务 | 已通过 | `MySQL84` Running，3307 TCP 可连接，未重启现有服务 |
| 验收数据库 | 已通过 | 独立 `xupan_platform_ws_accept_20260913` 完成验证，结束后已删除，未触碰正式库 |
| Spring Boot 健康端点 | 已通过 | 本地 8080 HTTP 200，状态 UP |
| 前端开发入口 | 已通过 | Playwright 访问本地 `/login`、`/room`，未记录会话参数 |

## 自动化验证

| 验证项 | 结果 | 证据/备注 |
| --- | --- | --- |
| 后端全量测试 | 已通过 | H2 `135/135`，0 failures、0 errors |
| 后端聊天室专项测试 | 已通过 | 相关专项 `34/34`，含连接权限复核和读游标权限 |
| 前端 Vitest | 已通过 | `11/11`，覆盖 ticket、订阅、同步去重、心跳、退避、清理、鉴权失败和 REST 降级 |
| 前端 typecheck | 已通过 | `npm run typecheck` |
| 前端 build | 已通过 | `npm run build`，静态资源已更新 |
| Flyway V1-V10 | 已通过 | 独立 MySQL 8.4.11 验收库连续迁移成功，当前 v10 |

## REST 与 WebSocket 运行态

| 验收项 | 结果 | 证据/备注 |
| --- | --- | --- |
| 登录与当前用户接口 | 已通过 | 本地管理员登录、`/me` 均 HTTP 200 |
| 聊天室列表与历史消息 | 已通过 | 本地 REST 均 HTTP 200；消息按序列查询成功 |
| 一次性 WebSocket ticket | 已通过 | 本地及服务器均 HTTP 200；原文未记录 |
| WebSocket 101 握手 | 已通过 | 本地直连和服务器公网 Nginx 均由 WebSocket 客户端成功 open |
| 双用户实时收发 | 已通过 | 本地 Playwright A/B 页面各收到 1 条；服务器 A/B 协议会话实时收发成功 |
| 刷新/断线序列补偿 | 部分通过 | Vitest 和 REST afterSequence/去重通过；页面刷新恢复脚本本次未完成，保留为未验证项 |
| 重复消息去重 | 已通过 | 本地协议验收重复 clientMessageId 返回 `deduplicated=true` |
| REST 降级模式 | 已通过 | 前端 Vitest 覆盖连接失败后的降级路径；未宣称浏览器断网实测 |
| 权限失效后的拒绝 | 已通过 | 本地停用 A 后既有连接关闭，关闭码 4401；服务端测试覆盖 4401/4403 |
| 禁言后的拒绝 | 已通过 | 既有 ChatMessageService/REST 禁言专项通过；未单独执行浏览器禁言操作 |
| XSS 纯文本显示 | 已通过静态检查 | `pre` 渲染，未使用 v-html、innerHTML 或 eval |

## 服务器发布与回滚

| 验收项 | 结果 | 证据/备注 |
| --- | --- | --- |
| 服务器发布前状态 | 已通过 | 记录了旧 Jar SHA、服务状态、Java/MySQL/Nginx、监听和 Flyway v10 |
| Nginx Upgrade 配置 | 已通过 | `/ws/` 含 HTTP/1.1、Upgrade、Connection、Host、75s timeout |
| Nginx `-t` 与 reload | 已通过 | `nginx -t` 成功；面板 Nginx 非 systemd 服务，使用受控 `nginx -s reload` |
| systemd 服务重启与健康检查 | 已通过 | `xupan-server.service` restart 后 active，健康 HTTP 200 |
| 服务器双浏览器最小回归 | 已通过 | 公网 Playwright 两隔离 context 在 375/1440 页面实时收发各 1 条且无横向溢出；临时账号已停用 |
| 发布失败演练 | 已通过 | 临时移除 `/ws/` 后握手未 open、error、close 1006 |
| 恢复上一版本/配置 | 已通过 | 从本次备份恢复带 WS 配置，`nginx -t` 成功并重新握手成功 |

## 未完成项与风险

- 页面刷新恢复、浏览器断网重连和服务器页面 DOM 回归仍需补充人工/浏览器验收后，才能把本阶段标为完全结项。
- 本次已验证的是单台 Spring Boot 实例、独立 MySQL 运行态、Nginx Upgrade 和受控发布回滚；不宣称多实例高可用或正式生产上线。
- 本计划不验证多实例广播、Redis/MQ、常驻机器人、聊天治理后台、真实支付或生产高可用。
- 若无法建立独立 MySQL 验收库、无法取得有效运行凭据或服务器回滚点不可确认，应保留阻塞证据并停止对应写入操作。

## 证据索引

| 编号 | 证据 | 位置/摘要 |
| --- | --- | --- |
| E-001 | Git、工具链和端口基线 | 本地 Java/Node/MySQL/3307 检查；提交 `69a3a67` |
| E-002 | 前端自动化测试输出 | Vitest `11/11`、typecheck、build |
| E-003 | 本地 MySQL/Flyway/REST/WS 输出 | 独立库 v10、健康 200、REST 200、本地协议双连接 |
| E-004 | 双浏览器验收结果 | Playwright 两隔离 context，375/1440 页面实时收发，DOM 各 1 条，无横向溢出 |
| E-005 | 服务器发布与回滚输出 | Jar SHA `31d3...5308d`，Nginx `-t`/reload，WS 失败/恢复演练 |

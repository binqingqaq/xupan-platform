# 168开奖网首页第一阶段静态验收记录

## 1. 验收范围

- 验收对象：当前项目新增的 168 开奖网桌面静态展示首页。
- 访问路由：`/display`。
- 交付边界：固定图片、文字、期号、号码和广告内容；前端本地样式与交互。
- 明确未包含：后端接口、数据库、真实开奖数据、投注、登录、移动端适配和外部站点运行时请求。
- 验收日期：2026-09-14。

## 2. 实现结果

- 已新增 Vue 页面 `web/src/views/DisplayHomeStatic.vue`。
- 已新增页面专用样式 `web/src/styles/display-home-static.css`。
- 已新增本地 Logo、横幅、活动卡片和侧栏广告 SVG 资源。
- 已新增公开路由 `/display`，保留 `/` 到 `/room` 的原有行为。
- 已为 Spring Boot SPA 刷新回退增加 `/display` 和 `/display/`。
- 页面未新增 API 调用、数据库表、Flyway 迁移或正式业务类型。

## 3. 自动化验证

| 检查项 | 命令或方式 | 结果 |
| --- | --- | --- |
| 前端类型检查 | `npm run typecheck` | 通过 |
| 前端测试 | `npm run test:run` | 通过，2 个测试文件，21 个测试 |
| 前端生产构建 | `npm run build` | 通过，Vite 生成展示页异步 chunk |
| 后端全量测试 | `server/xupan-server/.\mvnw.cmd test` | 通过，178 个测试，0 失败 |
| 开发路由响应 | `http://127.0.0.1:5174/display` | HTTP 200，返回 HTML |
| 代码格式检查 | `git diff --check` | 通过；仅有换行符提示 |

## 4. 浏览器检查

### 4.1 已检查内容

- 开发服务器使用 Vite `5174` 端口，`5173` 已被已有进程占用，因此未抢占原端口。
- `/display` 页面能够正常渲染顶部工具条、Logo、橙色导航、横幅、新闻、活动卡片、偏好栏、分类区、推荐区、广告区、页脚和悬浮工具。
- 页面刷新后仍能渲染，浏览器标题为“168开奖网首页 - 静态展示”。
- 偏好“展开/收起”能够显示和隐藏静态提示内容。
- 彩种分类标签能够切换预置的静态卡片内容。
- 轮播会自动切换，也提供指示点手动切换。
- 页面按钮只显示静态演示提示，不跳转到外部投注、支付或未知站点。
- 图片均使用本地 `/display/` 资源路径。

### 4.2 截图和尺寸边界

- 已在当前浏览器窗口完成桌面预览，截图视口约为 `1280 x 720`。
- 预览中首屏未发现主要区域重叠、图片缺失或横向布局崩坏。
- 计划中的 `1366 x 768` 和 `1920 x 1080` 独立窗口截图本轮未执行，后续用户视觉评审时仍需补充确认。
- 移动端 `m.16894913.com` 未执行，按第一阶段计划明确延期。

## 5. 结论

- 第一阶段代码实现完成，满足“静态桌面首页 + 前端本地交互”的当前范围。
- 本阶段没有将临时展示内容固化为后端接口或数据库契约。
- 当前交付等待用户进行视觉评审；在评审前不自动进入后端接入、数据建模或移动端开发。
- 后续若继续正式化，需要另行确认真实数据来源、正式字段、权限边界、接口方式和部署方式。

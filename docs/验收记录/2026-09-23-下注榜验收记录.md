# 2026-09-23 下注榜验收记录

> 验收对象：`/console` 左侧“下注榜”
>
> 证据等级：本机后端 H2/Flyway、本机前端构建与浏览器、本机 MySQL 运行态；不是服务器或生产验收。

## 1. 验收范围

- 当前期 `普`、`托` 页签的数量、总积分和注单列表。
- `上期` 空白状态。
- `开奖` 最近 18 期、8 个番值列、状态和倒计时。
- `×` 可见但禁用。
- `/console` 左侧导航下方布局。
- 桌面和 375px 基础响应式。

## 2. 已执行检查

### 2.1 后端

- 执行 `server/xupan-server` 全量 `.\mvnw.cmd -q test`。
- 读取全部 Surefire XML，结果：65 个报告文件、`250/250` 测试通过、0 失败、0 错误、0 跳过。
- `BetBoardControllerTest` 覆盖：
  - `BOT` 数量为 3、总积分为 `307.00`。
  - `NORMAL` 数量为 1、总积分为 `116.00`。
  - `CANCELED` 注单不进入数量和列表。
  - 开奖历史返回 18 期。
  - 普通玩家访问返回 403。
  - 玩法文本重建结果，例如 `12角/80` 和 `23无1/116`。

### 2.2 前端

- `npm run test:run`：14 个测试文件、`85/85` 通过。
- `npm run typecheck`：通过。
- `npm run build`：通过。
- `betBoard.test.ts` 覆盖积分格式、服务端时间倒计时和番值颜色映射。
- `betBoardPanel.test.ts` 覆盖有注单时 `×` 存在、文本正确、按钮禁用且提示未开放。
- `adminShell.test.ts` 覆盖四个页签和 `×` 禁用属性。

### 2.3 本机运行态

- 本机 Spring Boot 健康端点返回 HTTP 200。
- 本机 MySQL 下访问下注榜接口返回成功。
- 浏览器 `/console` 实际显示：
  - 左侧导航下方存在“下注榜”。
  - `普(0)`、`托(0)`、`上期`、`开奖` 四个页签。
  - `普`/`托` 可切换并显示当前期无下注状态。
  - `上期` 面板为空白，不渲染伪造数据。
  - `开奖` 页签渲染 18 行历史，包含期号、8 个番值列和“已开”状态。
  - 开奖倒计时显示并按服务端阶段截止时间递减。
  - 375px 视口下页面级 `scrollWidth` 未超过 `clientWidth`，下注榜面板宽度为 340px，无页面级横向溢出。

## 3. 未执行检查

- 服务器现场部署和生产数据库迁移。
- 生产管理员权限配置和浏览器现场验收。
- 管理员撤单、虚拟积分退回、流水和审计链路。
- `上期` 榜单的数据口径和展示。
- 多管理员并发查看、超大当前期注单量、长期轮询容量和故障恢复。

## 4. 已知边界

- 下注榜是只读查询，不改变注单、钱包或开奖状态。
- `×` 当前只保留视觉入口，禁用是既定行为，不是缺陷。
- 取消注单不计入数量和总积分，避免把已撤销下注显示为有效下注。
- 开奖表格展示的是四个番值颜色对应的番值 1 到 4，不是实际号码；实际号码保留在接口返回值中作为提示。
- 当前验收不能替代服务器或生产验证。

## 5. 结论

本机环境下，下注榜的四页签、当前期普/托统计、当前期明细、18 期开奖、倒计时、布局和禁用叉号均已形成可重复证据。当前阶段可以进入后续浏览器或现场验收；管理员撤单和 `上期` 榜单必须另立计划和验收边界。

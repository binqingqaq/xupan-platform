# 2026-09-23 玩家快捷下注面板与玩家偏好验收记录

> 验收对象：玩家页右上角“快捷”完整赔率与快捷下注面板
>
> 证据等级：H2/Flyway、本机 MySQL 8.4、前端构建和本机浏览器；不是服务器或生产验收。

## 1. 验收范围

- 旧快捷下拉是否完全移除。
- 74 个快捷赔率项、分组、顺序和滚动。
- 多选、取消、金额追加、`X` 回退和重置。
- 快速下注组合文本。
- 玩家级五个快捷金额偏好。
- 桌面和 375px 布局。
- `车`和短格式`无`的解析与结算。

## 2. 已执行检查

### 2.1 后端

- 后端全量 H2/Flyway：`252/252` 通过，0 失败、0 错误、0 跳过。
- Flyway 从 V27 迁移到 V28 成功。
- `PlayerQuickBetPreferenceControllerTest` 覆盖：
  - 未保存时返回默认 `50、100、200、500、1000`。
  - 保存五个金额后可重新读取。
  - 未认证返回 401。
  - 金额数量不正确返回 400。
- `BetTextParserTest` 覆盖：
  - `1车100`解析为覆盖 `2、3、4` 的车。
  - `3车151`解析成功。
  - `2无3/100`解析为 `2` 中、`3` 输。
  - 原有 `12无3/100`继续解析成功。
- `SettlementServiceTest` 覆盖短格式`无`的赢、输、和结果。

### 2.2 前端

- 前端全量 Vitest：16 个文件、`89/89` 通过。
- `quickBet.test.ts` 覆盖：
  - 快捷面板包含 74 项。
  - 全选并输入 `99` 后生成的文本与用户参考文本一致。
  - 组合顺序固定，不受点击顺序影响。
  - 数字追加、回退和默认金额。
- `quickBetPanel.test.ts` 覆盖：
  - 多选后选中数量正确。
  - 快捷金额替换输入值。
  - `X` 删除最后一位。
  - 重置清空赔率选择。
- `vue-tsc --noEmit` 和生产构建通过。

### 2.3 本机 MySQL 和浏览器

- 本机 MySQL 8.4 已执行 V28，创建玩家快捷金额偏好表。
- 浏览器实际打开玩家页，点击“快捷”后显示新面板。
- 桌面和 375px 均无页面级横向溢出。
- 主赔率拼图、下方赔率网格、底部快捷栏和滚动正常。
- 多选、取消、金额快捷按钮、数字追加、`X`和重置均验证通过。
- 五个金额改为 `10、20、30、40、50` 保存后，刷新页面仍恢复。
- 验证后已把本机测试账号快捷金额恢复为默认值。

### 2.4 本轮界面优化

- 点击`快捷`可展开赔率面板，再次点击可收起。
- 快捷面板打开时点击`界面`，选择框显示在赔率面板上方，没有被遮挡。
- 点击`账户流水`入口后，赔率面板关闭，账户流水打开。
- `界面1`/`界面2`中当前选中项显示勾选。
- 快捷面板下滑后关闭再打开，恢复上次退出时的滚动位置。
- 快捷、账户流水或历史界面刷新后统一回到普通下注首页。
- 设置保存后的金额更新底部快捷金额按钮，不影响`1`到`0`数字键盘。
- 设置弹窗已移除头像设置和`快捷金额`提示文字，只保留五个金额输入框和`保存`按钮。
- 设置弹窗中的`默认`按钮会把五个输入恢复为 `50、100、200、500、1000`，点击`保存`后持久化。
- 账户流水的已结算列表只查询当天，昨天或更早的已结算注单不再出现在该列表中。
- 账户流水按钮悬停时不再显示浏览器原生`账户流水`提示。
- 机器人文本消息气泡改为按正文长度自适应，短消息和长消息宽度不再固定一致。
- 375px 视口下五个金额和保存按钮可以同时显示，无页面级横向溢出。

## 3. 未执行检查

- 使用真实余额从页面提交全部 74 项的完整业务演练。
- 服务器现场部署和生产数据库迁移。
- 多设备同步、正式运营和长期容量观察。
- 管理员撤单和上期榜单。

## 4. 已知边界

- 快捷面板赔率来自当前项目服务端配置；若后台修改赔率，面板显示会同步变化。
- 快捷金额偏好只属于玩家界面配置，不代表余额、积分流水或资金事实。
- 组合下注沿用聊天室现有事务，任一注单失败时整批不生效。
- 旧快捷下拉代码保留但不再渲染。

## 5. 结论

本机环境下，玩家快捷下注面板、参考顺序组合文本、玩家级快捷金额偏好和新增玩法解析均已形成可重复证据。服务器、生产环境和真实全量下注演练仍待后续验收。

# Xupan Platform 产品总览与模块地图

> 文档类型：项目总控基线
>
> 当前基线日期：2026-09-24
>
> 代码基线：`3e431d2 feat: 完成上下分审批、下注榜、快捷下注与后台赔率限额配置`（工作区另含托自动行为改造等未提交改动）
>
> 使用规则：本文记录当前项目的产品边界、业务模块、依赖关系和开发阶段。它不是具体接口文档，也不是单次开发计划。新需求先归入本文的模块，再补充模块状态和业务流程。

## 1. 当前结论

项目已经从“首期玩法和聊天室基础能力建设”进入“运营后台、玩家前台和托自动行为细化”阶段。

当前可以确认：

- Spring Boot 后端、Vue 前端、Flyway 数据库迁移和 MySQL 8.4 LTS 技术基线已经建立。
- 管理员、普通玩家、TEST 玩家、BOT 和服务端机器人已经形成不同身份边界。
- 玩家链接登录、软删除、虚拟积分、下注结算、聊天室、WebSocket、机器人和玩家工作台已经形成可联动的业务闭环。
- 运营后台已包含玩家工作台、上下分审批、最近记录、下注榜、赔率与限额配置、报网与线路静态区域、封盘与显示控制区域。
- 玩家前台已包含参考式快捷下注面板、玩家级快捷金额偏好、最近 4 小时聊天历史与新消息提示，以及`说明`指令（发送后仅本机弹窗）。
- 托自动行为已按确认规则重做：链接是工作前提、期内随机时间下注、11 类指定玩法、下注范围档位与金额整十、活跃比例 A+B、所有托每期总注单/总积分上限、随机上分申请和余额不足降额。
- 当前仍是单服务器、单 Spring Boot 实例方案；不能把本机验证写成服务器或生产验收。

当前不应再采用“想到一个页面就直接修改一个接口”的方式。任何新功能都要先确认它属于哪个模块、影响哪些主流程、产生哪些持久化事实，以及如何验收。

## 2. 产品边界

### 2.1 已确认边界

- 产品定位为合法合规的虚拟积分娱乐产品。
- 余额、下注额、返还、盈亏、积分流水和结算全部表示虚拟积分。
- 不接入充值、支付、提现、转账、代理分成或现实资金结算。
- TEST 玩家和 BOT 必须有可见、可查询、可审计的身份标记，不能伪装成普通真实用户。
- 数据库是业务事实源；前端计算、浏览器状态、内存状态和临时文件不能作为唯一事实。
- 关键操作必须考虑权限、事务、幂等、审计、失败恢复和重复执行。

### 2.2 当前范围

- 一个单体 Spring Boot 服务。
- 一个单服务器、单实例运行基线。
- 一个当前游戏和期号流，已有期号、玩法、下注、开奖、结算和虚拟积分链路。
- 管理员后台、玩家工作台、普通玩家链接前台、聊天室和展示端页面。
- 服务端常驻机器人和 TEST/BOT 自动行为。

### 2.3 当前不纳入或尚未确认

- 现实资金能力和可兑换资产。
- Redis、多实例协调、分布式调度锁和跨节点 WebSocket 广播。
- 第三方开奖接口或未经确认的生产开奖源。
- 抽水、飞单、代理分成和现实资金语义。
- 总结果聚合、特玩法金额口径、和局返还等仍需确认的业务规则。

## 3. 技术与部署基线

```text
Vue/Vite 前端
    -> REST API / WebSocket
Spring Boot 单体服务
    -> 领域服务、权限、调度、消息和审计
    -> JDBC Repository
MySQL 8.4 LTS（正式事实源）
    -> Flyway 迁移
```

- Java：21。
- 后端：Spring Boot，静态资源可由同一服务托管。
- 前端：Vue 3、TypeScript、Vite。
- 数据库：正式基线为 MySQL 8.4 LTS，测试可使用 H2，但 H2 结果不能替代 MySQL、服务器或生产证据。
- 当前运行决策：单服务器、单实例；扩容、Redis 和分布式能力必须以真实容量或故障证据为依据另行评估。

## 4. 业务模块地图

| 模块 | 主要职责 | 关键事实 | 主要依赖 | 当前状态 |
| --- | --- | --- | --- | --- |
| 平台基础与发布 | 配置、Flyway、日志、健康检查、指标、启动、备份和故障排查 | 迁移版本、运行配置、审计和运行日志 | 所有模块 | 基础已建立，服务器/生产收口未完成 |
| 身份与权限 | 管理员登录、普通玩家链接登录、TEST/BOT 身份、角色权限、会话失效 | `sys_user`、会话、玩家链接、权限、身份审计 | 平台基础 | 本机收口完成 |
| 玩家工作台 | 玩家列表、创建、详情、会员编号、昵称、头像、拉黑/拉白链接、软删除 | 用户、玩家账户、会员编号、头像和状态 | 身份、虚拟积分 | 本机功能已形成，部分现场验收待补 |
| 上下分申请与审批 | 聊天上下分申请、待审批队列、同意/拒绝、提示音和最近记录 | `player_point_request`、钱包流水 | 聊天、虚拟积分、运营后台 | 本机闭环完成，服务器和生产待验收 |
| 后台赔率与风控配置 | 赔率/特码赔率/特码返水自动保存、13 项限额弹窗、所有托每期总注单与总积分、深夜活跃比例覆盖 | `game_betting_config`、`game_odds` | 运营后台、游戏核心、托行为 | 本机闭环完成；多实例强一致待补 |
| 虚拟积分与流水 | 上分、下分、下注扣分、结算返还、余额查询、流水审计 | 钱包余额、`demo_balance_ledger`、下注和结算 | 身份、游戏核心 | 业务闭环已实现，环境分层验收仍需持续 |
| 游戏核心 | 期号、赔率、玩法解析、下注、开奖、结算和注单查询 | `game_issue`、赔率、`game_bet`、结算状态 | 身份、虚拟积分 | 首期链路已实现，完整现场周期仍需补验 |
| 下注榜与开奖视图 | 当前期普/托注单数量、总积分、明细、最近 18 期开奖和倒计时 | `game_bet`、`game_issue` 和 `demo_user_account.player_kind` 的只读投影 | 游戏核心、玩家分类、运营后台 | 本机闭环完成；撤单、上期榜单和现场验收待后续 |
| 玩家快捷下注与偏好 | 参考式完整赔率面板、74 项多选、底部快捷栏、组合下注文本和玩家级快捷金额 | `game_odds`、`game_bet`、`player_quick_bet_preference` | 游戏核心、虚拟积分、玩家前台 | 本机闭环完成；服务器、生产和全量真实下注演练待后续 |
| 聊天与实时通信 | 房间、消息、已读游标、禁言、outbox、WebSocket、4 小时历史窗口、新消息提示和降级 | `chat_message`、outbox、序号、连接和会话 | 身份、平台基础 | 单实例运行态已验证，生产高可用未完成 |
| 服务端机器人 | 游戏事件消息、模板、开奖图像、重试、幂等和审计 | 机器人身份、模板、dispatch、消息 | 游戏核心、聊天 | 基础能力已实现，部分浏览器和现场专项待补 |
| TEST/BOT 自动行为 | 链接工作前提、期内随机时间下注、玩法与金额档位、活跃比例、全局每期上限、随机上分申请、租约和重试 | `test_player_behavior`、`test_player_behavior_play_type`、`test_player_action` | 玩家工作台、游戏、聊天、后台配置 | 本机实现和测试完成；多实例强一致与现场周期待补 |
| 展示端与玩家前台 | PC/移动首页、期号与外部开奖展示、玩家房间、快捷下注、账户流水和说明弹窗 | 展示查询和前台会话 | 游戏、身份、聊天 | 页面复刻和前台链路分开维护 |
| 运营报表 | 积分记录、业务日汇总、注单和动作明细 | 报表查询结果，不新增第二套事实源 | 玩家、钱包、游戏、BOT | 积分记录已实现，MySQL/现场复验待补 |

## 5. 模块依赖关系

```text
平台基础
  -> 身份与权限
       -> 玩家工作台
       -> 普通玩家前台
       -> TEST/BOT 自动行为
  -> 虚拟积分
       -> 下注与结算
       -> 积分报表
  -> 游戏核心
       -> 聊天反馈
       -> 服务端机器人
       -> TEST/BOT 自动行为
  -> 聊天与实时通信
       -> 玩家前台和机器人消息
```

关键原则：

- 钱包余额和流水由服务端交易链路产生，不能由页面直接计算或写入。
- 下注必须依赖当前期号、玩家身份、玩法校验和钱包余额；不能把下注做成单独页面假数据。
- 机器人消息是聊天事实或系统事件反馈，不能替代注单、钱包、结算等业务事实。
- 报表只能读取已有业务事实，不能再建一套与钱包、注单相冲突的统计事实源。
- 删除玩家不删除业务历史；必须撤销会话、链接和后续行为，并保留可审计关联。

## 6. 当前开发阶段

### 已完成的阶段性基础

- 首期游戏、期号、玩法、下注、随机开奖和结算基础。
- 虚拟积分钱包、管理员分配、下注扣分、结算和流水。
- 聊天 REST 持久化、WebSocket、断线补偿和单实例实时消息。
- 服务端机器人、开奖消息、模板、重试和审计基础。
- 统一管理后台和玩家工作台。
- TEST 玩家、BOT 分类和自动/手动行为闭环。
- 玩家身份字段、链接登录、链接刷新、过期/撤销、软删除和历史保留。
- 玩家积分记录弹窗和按 `Asia/Shanghai` 每天 06:00 切换的业务日统计。
- 聊天室上下分申请、后台确认、两种提示音、玩家/托最近记录和手动上下分联动已在本机形成闭环。
- `/console` 左侧下注榜已支持当前期普/托聚合、当前期明细、空白上期、18 期开奖和倒计时，`×` 保留但禁用。
- 玩家页“快捷”已替换为参考式完整赔率与快捷下注面板，支持 74 项多选、组合下注文本和玩家级快捷金额。
- 管理员和玩家链接使用独立刷新 Cookie，同一浏览器的管理员标签页与玩家标签页可同时保持会话。
- 后台顶部赔率、特码赔率、特码返水已可编辑自动保存；限额弹窗已包含 13 项玩家限额和所有托每期总注单/总积分、深夜活跃比例覆盖。
- 聊天室玩家历史窗口限制为最近 4 小时，并提供右下角新消息提示与点击回到最新消息。
- 托自动行为已改为「链接是工作前提 + 期内随机时间下注 + 自动模式不聊天 + 金额档位与整十 + 11 类指定玩法 + 活跃比例 + 全局每期上限 + 随机上分申请」。
- 玩家工作台移除了长期不用的「停用玩家/启用玩家」入口，保留后端状态接口与处理函数备用。
- 后台底部新增封盘与显示控制区域（预留封盘/取消/返水、显示右角/倒计时开关、移上），当前为纯前端交互。

### 当前阶段定位

当前处于：**后台配置、下注榜、快捷下注、上下分审批和托自动行为本机闭环完成，准备处理服务器现场、真实全量下注、多实例强一致、撤单专项和生产发布收口。**

这不是正式生产上线状态。仍需单独完成或持续补充：

- 服务器现场部署、迁移、重启、回滚和备份恢复演练。
- 生产数据库、HTTPS、监控告警和正式运维链路。
- 托完整开奖周期、多人聊天室和关键页面的现场复验。
- 多实例部署下玩家限额和托全局上限的强一致控制。
- 玩家工作台新托面板的 375px 与桌面截图复验。
- 链接元数据保留期限和定期清理策略。
- 尚未确认的游戏规则和后续运营模块。

## 7. 新需求进入规则

新增需求必须先补充以下内容，再进入具体编码计划：

1. 所属模块和关联模块。
2. 影响的业务流程、角色、接口、数据表和页面。
3. 新增或改变的业务规则。
4. 成功、失败、重复请求和中断恢复行为。
5. 数据库事实、权限和审计要求。
6. H2、本机 MySQL、服务器和生产分别需要什么证据。

具体实施仍写入 `docs/plans/`；验收证据仍写入 `docs/验收记录/`。如果总控文档与代码不一致，以当前源代码、迁移和测试结果为准，并立即补充状态差异。

# 首期玩法演示验收记录

## 验收信息

- 验收日期：2026-09-08
- 验收范围：首期 8 球番摊玩法演示
- 运行服务：Spring Boot `4.0.8`，`http://127.0.0.1:8080`
- 数据库：MySQL Community Server `8.4.11`，`xupan_platform`，端口 `3307`
- 测试数据库：H2 `2.4`，启用 Flyway

## 自动化测试

使用 JDK 21 和已缓存 Maven 3.9.16 执行完整测试套件：

```text
Tests run: 11, Failures: 0, Errors: 0
BUILD SUCCESS
```

覆盖内容：

- 号码到番、单双、大小的领域映射。
- 番、角、车、严、加、正、通、无、单双、大小、特的结算分支。
- 投注额两位小数、赔率三位小数和非法参数拒绝。
- Flyway 建表、期号、赔率、下注、赔率快照和一次性结算。
- Controller 的成功请求、非法金额、非法球位、非法号码、重复开奖和封盘下注。

## MySQL 验收

Spring Boot 使用本机环境变量连接 MySQL，启动日志确认：

- MySQL `8.4` 连接成功。
- Flyway V1 迁移成功。
- `flyway_schema_history`、`game_issue`、`game_odds`、`game_bet` 已创建。
- `game_bet` 结算更新包含 `settlement_status = 'PENDING'` 和 `settled_at IS NULL` 条件。

## 四类玩法实测

在 `DEMO-0002` 期提交以下注单，并录入开奖号码 `[1, 2, 12, 4, 5, 6, 7, 8]`：

| 球位 | 玩法 | 参数 | 投注额 | 赔率快照 | 状态 | 净盈亏 |
| --- | --- | --- | ---: | ---: | --- | ---: |
| 第 1 球 | 番 | 1 | 10.00 | 3.850 | WIN | 28.50 |
| 第 2 球 | 角 | 1,2 | 10.00 | 1.950 | WIN | 9.50 |
| 第 3 球 | 大小 | 大 | 10.00 | 1.950 | WIN | 9.50 |
| 第 4 球 | 特 | 4 | 10.00 | 18.000 | WIN | 170.00 |

实测同时确认：

- 重复调用开奖接口返回 HTTP `400`。
- 关闭期提交下注返回 HTTP `400`。
- 点击“开启下一期”后生成 `DEMO-0003`，状态恢复为 `OPEN`，历史期号和注单保留。

## 页面验收

- 页面由 Spring Boot 静态资源提供，浏览器可以直接打开首页。
- 页面展示期号、状态、8 个球位、号码、番、大小、单双、赔率、玩法参数、下注记录和结算结果。
- 页面支持球位选择、玩法选择、快捷参数、赔率调整、录入开奖和开启下一期。
- 已通过窄视口截图检查 4 列球位和 4 列开奖输入布局；响应式 CSS 在桌面宽度使用 8 列球位布局，在窄视口切换为 4 列布局。
- 未发现页面级横向滚动；注单表在窄屏仅允许表格区域横向查看，页面主体不被撑宽。

## 未纳入本阶段

真实用户、生产权限、实时通信、聊天机器人和第三方开奖接口均未实现。本轮已实现演示自动轮期：每期 300 秒，前 180 秒下注、后 120 秒开奖中，首期从 `3000000` 开始，8 个号码允许重复随机生成。特玩法本金口径、和局返还和总结果聚合仍需甲方确认。
